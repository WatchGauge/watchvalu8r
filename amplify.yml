version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm -v
        - npm ci || npm install
    build:
      commands:
        - npx next telemetry disable
        - npm run build
# 👇 IMPORTANT: Do NOT define artifacts or cache here.
# Amplify will auto-detect a Next.js app and deploy it with SSR.







